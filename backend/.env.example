# Database Configuration
# For local development: PostgreSQL connection string
# For production (Railway/Neon): Use the provided DATABASE_URL from your service
DATABASE_URL=postgresql+asyncpg://apple:MyStrongPassword123@localhost:5432/hackathon_platform
# Redis Configuration (Optional - only if using Redis for caching)
REDIS_URL=redis://localhost:6379/0

# JWT Configuration
# Generate secure keys with: openssl rand -hex 32
JWT_SECRET_KEY=your_jwt_secret_key_here_generate_with_openssl_rand_hex_32
REFRESH_SECRET_KEY=your_refresh_secret_key_here_generate_with_openssl_rand_hex_32

JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
REFRESH_TOKEN_EXPIRE_DAYS=7

# Qdrant Vector Database Configuration
# Sign up at https://qdrant.tech/ for free tier (1GB storage)
QDRANT_URL=your_qdrant_cluster_url_here
QDRANT_API_KEY=your_qdrant_api_key_here
QDRANT_COLLECTION_NAME=Humanoid

# Cohere Configuration (Optional - for embeddings)
# Sign up at https://cohere.com/ for API key
COHERE_API_KEY=your_cohere_api_key_here
COHERE_MODEL=embed-english-v3.0

# OpenAI Configuration (Required for RAG and personalization)
# Sign up at https://platform.openai.com/ for API key
OPENAI_API_KEY=your_openai_api_key_here

# Claude Configuration (Optional - alternative to OpenAI)
CLAUDE_API_KEY=your_claude_api_key_here

# CORS Configuration
# Add your frontend URLs (comma-separated)
# For production: https://github.com/tahirbaba.io
# For local dev: http://localhost:3000
CORS_ORIGINS=http://localhost:3000,http://localhost:8000

# Application Configuration
APP_NAME=Physical AI Interactive Textbook
APP_VERSION=1.0.0
DEBUG=False

# Frontend URL (for CORS and callbacks)
FRONTEND_URL=http://localhost:3000
